import React from 'react';
 import { useState } from 'react';

const SliderForm = ({ title, fields, onSubmit }) => {

  return (

    <>
      {
      // <div className="card card_container mb-4">
      //   <h4 className="heading_titles purpleColor mb-3">{title}</h4>
      //   <form onSubmit={onSubmit}>
      //     <div className="row">
          
      //       {fields.map((field, index) => (
      //         <div key={index} className={`col-md-${field.col || 6} mb-4`}>
      //           <div className="formcontrol2">
      //             <label>{field.label}</label>
      //             <input
      //               type={field.type}
      //               className="form-input"
      //               placeholder={field.placeholder}
      //               name={field.name}
      //             />
      //           </div>
      //         </div>
      //       ))}

      //       <div className="col-12 d-flex justify-content-end">
      //         <button type="submit" className="submit_button_admin">
      //           Submit
      //         </button>
      //       </div>
      //     </div>
      //   </form>  
      // </div>  
}
   

  </>

  );
};

export default SliderForm;
